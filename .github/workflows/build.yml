name: Build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: Docker Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Build Docker image
        run: docker build -t fizzbuzz-api .
